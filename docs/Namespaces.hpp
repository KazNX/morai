#pragma once

/// @namespace morai
/// The primary namespace of the Morai fibre library.

/// @namespace morai::log
/// Logging interface. Provides a @c LogHook which can be set via @c setHook(). All log calls go via
/// this hook. The default logs to stdout.

/// @namespace weaver
/// A text mode rendering API used in the Morai examples.
